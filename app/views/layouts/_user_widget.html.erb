<% if user_id_signed_in? %>
  <p>Welcome <%= current_user_id.email %></p>
  <%= link_to 'Logged In [click to logout]', destroy_user_id_session_path, :method => :delete %>
<% else %>
  <p>You are not signed in.</p>
  <%= link_to 'Login', new_user_id_session_path %>
<% end %>